import type { CoachAPI } from '../types'

// Stub — replaced by the classification/heuristics/explanation engine.
export function createCoach(): CoachAPI {
  throw new Error('coach not implemented yet')
}
