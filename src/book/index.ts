import type { Opening, OpeningBook } from '../types'

// Stub — replaced by the history-keyed opening book.
export function createBook(_openings: Opening[]): OpeningBook {
  throw new Error('book not implemented yet')
}
